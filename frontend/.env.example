NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Set this to your client's storeUrl (assigned by the platform owner)
# Each client deployment gets their own unique storeUrl
NEXT_PUBLIC_STORE_URL=your_store_url_here
